<!DOCTYPE html>
<html style="background-color: #F0F4F8">
  <head>
    <title>Mementos: Personalized Photo Albums</title>
    <meta name="description" content="Personalized photo albums in just a few minutes. Show your loved ones where you've been.">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= favicon_link_tag "favicon.ico" %>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "https://unpkg.com/dropzone@5/dist/min/dropzone.min.css" %>
    <%= javascript_importmap_tags %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.5.0/font/bootstrap-icons.css" rel="stylesheet" />
    <link rel="preconnect" href="https://fonts.gstatic.com" />
    <link href="https://fonts.googleapis.com/css2?family=Newsreader:ital,wght@0,600;1,600&amp;display=swap" rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css2?family=Mulish:ital,wght@0,300;0,500;0,600;0,700;1,300;1,500;1,600;1,700&amp;display=swap" rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css2?family=Kanit:ital,wght@0,400;1,400&amp;display=swap" rel="stylesheet" />

    <meta property="og:title" content="Mementos: Personalized photo albums in a few minutes." />
    <meta property="og:description" content="Personalized photo albums in just a few minutes. Show your loved ones where you've been." />
    <meta property="og:image" content="https://divo-photobook-pdf-public.s3.eu-west-1.amazonaws.com/example_album_reduced.jpg" />
    <meta property="og:url" content="https://mementos.ink" />

    <% if defined?(@home) %>
    <% end %>
  </head>

  <body class="d-flex flex-column vh-100">
    <% unless @mobile_app %>
      <%= render "shared/nav" %>
    <% end %>
    <%= yield %>

    <dialog id="turbo-confirm" class="dialog">
        <div class="dialog__content">
          <form method="dialog">
            <p class="dialog__title" id="dialog-message">Are your sure?</p>
            <div>
              <button value="cancel" class="btn btn-secondary">Cancel</button>
              <button value="confirm" class="btn btn-danger">Confirm</button>
            </div>
          </form>
        </div>
    </dialog>
    <%= render "layouts/contact" %>
  </body>
</html>
